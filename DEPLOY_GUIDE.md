# 1Panel 完整部署指南

本文档详细说明如何将数据分析 Agent 项目部署到 1Panel，包括打包、上传、配置和启动的完整流程。

---

## 📋 项目架构说明

### 项目组成

本项目包含 **两个服务**，需要 **一起部署**：

| 服务            | 技术栈     | 端口 | 说明                             |
| --------------- | ---------- | ---- | -------------------------------- |
| **Web 服务**    | Next.js 16 | 3000 | 前端界面 + API 路由 + 数据库操作 |
| **Python 服务** | FastAPI    | 8000 | 数据分析 Agent + Excel 处理      |

### 服务关系图

```
用户浏览器
    │
    ▼
┌─────────────────────────────────────┐
│  Nginx反向代理 (可选，用于域名访问)   │
│  端口: 80/443                        │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Next.js Web服务                     │
│  端口: 3000                          │
│  - 前端页面渲染                       │
│  - API路由 (/api/*)                  │
│  - 用户认证                          │
│  - SQLite数据库                      │
└─────────────────────────────────────┘
    │
    │ 内部调用 (http://python-service:8000)
    ▼
┌─────────────────────────────────────┐
│  Python FastAPI服务                  │
│  端口: 8000                          │
│  - 数据分析Agent                     │
│  - Excel文件处理                     │
│  - LLM集成 (可选)                    │
└─────────────────────────────────────┘
```

### ❓ 前端和后端需要分别部署吗？

**答案：两个服务需要一起部署，但使用 Docker Compose 可以一键完成。**

- **不能只部署其中一个**，因为：
  - Web 服务需要调用 Python 服务进行数据分析和 Excel 处理
  - Python 服务需要 Web 服务提供回调接口

- **推荐方式**：使用 Docker Compose 一键部署两个服务（最简单）
- **备选方式**：分别创建两个 Docker 容器（更灵活）

---

## 🔧 第一步：准备工作

### 1.1 服务器要求

| 项目     | 最低要求               | 推荐配置         |
| -------- | ---------------------- | ---------------- |
| 操作系统 | Linux (Ubuntu 20.04+)  | Ubuntu 22.04 LTS |
| CPU      | 1 核                   | 2 核+            |
| 内存     | 2GB                    | 4GB+             |
| 磁盘     | 10GB                   | 20GB+            |
| 软件     | 已安装 1Panel + Docker | -                |

### 1.2 在本地准备项目文件

#### 方式 A：压缩上传（推荐新手）

在 Windows 上：

1. 打开项目文件夹 `data_analysis_agent`
2. **删除以下目录**（减小体积，这些会在服务器重新生成）：
   - `node_modules/`
   - `.next/`
   - `__pycache__/`
3. 选中所有文件，右键 -> 发送到 -> 压缩文件夹
4. 得到 `data_analysis_agent.zip`

#### 方式 B：使用 Git（推荐）

```bash
# 在项目目录执行
git add .
git commit -m "准备部署"
git push origin main
```

---

## 📤 第二步：上传项目到服务器

### 方式 A：通过 1Panel 文件管理（最简单）

1. 登录 1Panel 管理面板
2. 点击左侧「文件」
3. 导航到 `/opt/` 目录
4. 点击「上传」按钮，选择 `data_analysis_agent.zip`
5. 等待上传完成
6. 右键点击压缩包 -> 「解压」
7. 确保解压后的目录名为 `data_analysis_agent`

### 方式 B：通过 Git 克隆

```bash
# SSH 登录服务器
ssh root@你的服务器IP

# 进入目标目录
cd /opt

# 克隆项目
git clone https://github.com/你的用户名/data_analysis_agent.git
```

### 方式 C：通过 SCP 上传

```bash
# 在本地 Windows PowerShell 执行
scp data_analysis_agent.zip root@你的服务器IP:/opt/

# SSH 登录服务器后解压
ssh root@你的服务器IP
cd /opt
unzip data_analysis_agent.zip
```

---

## ⚙️ 第三步：配置环境变量

### 3.1 创建环境变量文件

```bash
# SSH 登录服务器
ssh root@你的服务器IP

# 进入项目目录
cd /opt/data_analysis_agent

# 复制环境变量模板（如果存在）
cp env.production.template .env

# 或者直接创建
nano .env
```

### 3.2 编辑 .env 文件

```bash
# ============================================
# 必须修改的配置
# ============================================

# 应用访问地址（改为你的服务器IP或域名）
NEXT_PUBLIC_APP_URL=http://你的服务器IP:3000

# JWT密钥（必须修改！用于用户认证加密）
# 生成方法：openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-here-replace-with-random-string-32chars

# ============================================
# 可选配置
# ============================================

# 端口配置（默认即可）
WEB_PORT=3000
PYTHON_PORT=8000

# LLM配置（如果需要AI分析功能）
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=你的OpenAI_API_Key
LLM_MODEL=gpt-4o-mini
```

**保存文件**：按 `Ctrl+X`，然后按 `Y`，最后按 `Enter`

### 3.3 生成 JWT 密钥

```bash
# 生成随机密钥
openssl rand -base64 32

# 复制输出的字符串，填入 .env 的 JWT_SECRET
```

---

## 📁 第四步：创建数据目录

```bash
# 创建数据持久化目录
mkdir -p /opt/data_analysis_agent/data/prisma
mkdir -p /opt/data_analysis_agent/data/uploads
mkdir -p /opt/data_analysis_agent/data/python_uploads

# 设置权限（确保容器可以读写）
chmod -R 777 /opt/data_analysis_agent/data
```

---

## 🚀 第五步：构建和启动服务

### 方式一：使用 Docker Compose（强烈推荐）

这是最简单的方式，一条命令启动所有服务。

```bash
# 进入项目目录
cd /opt/data_analysis_agent

# 构建并启动所有服务（首次需要5-15分钟）
docker-compose up -d --build

# 查看启动状态
docker-compose ps

# 查看实时日志（确认没有错误）
docker-compose logs -f

# 按 Ctrl+C 退出日志查看
```

**预期输出：**

```
NAME                    STATUS
data_analysis_agent-web-1           Up
data_analysis_agent-python-service-1 Up
```

### 方式二：在 1Panel 图形界面操作

1. 登录 1Panel
2. 进入「容器」->「编排」
3. 点击「创建编排」
4. 选择「从 Compose 文件创建」
5. 名称：`data-analysis-agent`
6. 路径：`/opt/data_analysis_agent`
7. 点击「确定」
8. 找到创建的编排，点击「启动」

---

## 🗄️ 第六步：初始化数据库

首次部署需要初始化数据库表结构：

```bash
# 进入 Web 容器
docker-compose exec web sh

# 在容器内执行数据库初始化
npx prisma db push

# 看到 "Your database is now in sync" 表示成功

# 退出容器
exit
```

**或者一条命令完成：**

```bash
docker-compose exec web npx prisma db push
```

---

## ✅ 第七步：验证部署

### 7.1 检查服务状态

```bash
# 查看容器状态
docker-compose ps

# 两个容器都应该是 "Up" 状态
```

### 7.2 测试服务连通性

```bash
# 测试 Web 服务
curl http://localhost:3000

# 测试 Python 服务
curl http://localhost:8000/health
```

### 7.3 浏览器访问

打开浏览器，访问：`http://你的服务器IP:3000`

- 应该看到登录页面
- 首次使用，点击注册创建第一个用户（将自动成为管理员）
- **重要**：第一个注册的用户名必须是「卢金旭」才能拥有管理员权限

---

## 🌐 第八步：配置域名访问（可选但推荐）

如果你有域名，可以配置更友好的访问方式。

### 8.1 在 1Panel 创建反向代理

1. 登录 1Panel
2. 进入「网站」->「创建网站」
3. 选择「反向代理」
4. 配置：
   - **主域名**：`your-domain.com`
   - **代理地址**：`http://127.0.0.1:3000`
5. 点击「确定」

### 8.2 配置 HTTPS（推荐）

1. 在网站列表找到刚创建的网站
2. 点击进入设置
3. 点击「HTTPS」
4. 选择「申请证书」-> Let's Encrypt
5. 填写邮箱，点击申请

### 8.3 更新环境变量

```bash
# 编辑 .env 文件
nano /opt/data_analysis_agent/.env

# 修改 NEXT_PUBLIC_APP_URL 为你的域名
NEXT_PUBLIC_APP_URL=https://your-domain.com

# 保存后重启服务
docker-compose restart
```

---

## 🔄 日常维护

### 查看日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 只看 Web 服务日志
docker-compose logs -f web

# 只看 Python 服务日志
docker-compose logs -f python-service
```

### 重启服务

```bash
# 重启所有服务
docker-compose restart

# 只重启某个服务
docker-compose restart web
docker-compose restart python-service
```

### 停止服务

```bash
# 停止所有服务
docker-compose down

# 停止并删除数据卷（谨慎！会删除数据）
docker-compose down -v
```

### 更新部署

```bash
cd /opt/data_analysis_agent

# 如果使用 Git
git pull

# 重新构建并启动
docker-compose down
docker-compose up -d --build

# 如果数据库结构有变化
docker-compose exec web npx prisma db push
```

---

## 💾 数据备份

### 重要数据位置

| 数据     | 路径                                          | 重要性          |
| -------- | --------------------------------------------- | --------------- |
| 数据库   | `/opt/data_analysis_agent/data/prisma/dev.db` | ⭐⭐⭐ 必须备份 |
| 上传文件 | `/opt/data_analysis_agent/data/uploads/`      | ⭐⭐ 建议备份   |

### 手动备份

```bash
# 创建备份目录
mkdir -p /opt/backups

# 备份数据库
cp /opt/data_analysis_agent/data/prisma/dev.db /opt/backups/dev_$(date +%Y%m%d).db

# 备份上传文件
tar -czf /opt/backups/uploads_$(date +%Y%m%d).tar.gz /opt/data_analysis_agent/data/uploads
```

### 自动备份（推荐）

```bash
# 创建备份脚本
cat > /opt/backup_data_analysis.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/backups/data_analysis"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp /opt/data_analysis_agent/data/prisma/dev.db $BACKUP_DIR/dev_$DATE.db
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz /opt/data_analysis_agent/data/uploads
find $BACKUP_DIR -mtime +7 -delete
echo "Backup completed: $DATE"
EOF

# 添加执行权限
chmod +x /opt/backup_data_analysis.sh

# 测试运行
/opt/backup_data_analysis.sh
```

在 1Panel 中设置定时任务：

1. 进入「计划任务」
2. 创建任务，类型选择「Shell 脚本」
3. 脚本内容：`/opt/backup_data_analysis.sh`
4. 执行周期：每天凌晨 3 点

---

## 🔍 常见问题排查

### Q1: 容器启动失败

```bash
# 查看详细错误
docker-compose logs web
docker-compose logs python-service
```

### Q2: 数据库错误

```bash
# 检查数据库文件
ls -la /opt/data_analysis_agent/data/prisma/

# 修复权限
chmod 666 /opt/data_analysis_agent/data/prisma/dev.db
```

### Q3: 端口被占用

```bash
# 检查端口占用
netstat -tlnp | grep 3000
netstat -tlnp | grep 8000

# 杀死占用进程或修改 docker-compose.yml 中的端口
```

### Q4: 内存不足

```bash
# 查看内存使用
free -h

# 查看 Docker 内存使用
docker stats
```

### Q5: 无法访问

1. 检查防火墙是否开放端口 3000
2. 检查云服务器安全组是否开放端口
3. 检查容器是否正常运行

---

## 📝 部署检查清单

完成部署后，请确认以下事项：

- [ ] 两个容器都在运行 (`docker-compose ps`)
- [ ] Web 服务可访问 (浏览器打开 http://IP:3000)
- [ ] 可以注册第一个用户
- [ ] 可以登录系统
- [ ] 可以上传文件
- [ ] Excel 处理功能正常
- [ ] 已修改 JWT_SECRET
- [ ] 已设置数据备份

---

## 🛡️ 安全建议

1. **修改 JWT 密钥**：务必修改默认的 JWT_SECRET
2. **配置防火墙**：只开放 80/443 端口，3000/8000 端口仅内网访问
3. **使用 HTTPS**：通过 1Panel 申请 SSL 证书
4. **定期备份**：设置自动备份任务
5. **定期更新**：关注项目更新，及时升级

---

## 📞 需要帮助？

如遇到问题，请提供以下信息：

1. `docker-compose logs` 输出
2. 服务器配置（内存、CPU）
3. 1Panel 版本
4. 具体错误信息截图
